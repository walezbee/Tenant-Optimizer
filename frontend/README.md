# Frontend (React) — Tenant Optimizer

## Overview
React SPA for Tenant Optimizer. Features:
- Dashboard
- Resource lists (orphaned, deprecated)
- Approvals workflow
- Reports

## Run Locally

```bash
npm install
npm start
```

## Configuration
- Edit `src/api/index.ts` to point to your backend URL if running locally.